<%= form_for(@keypair) do |f| %>
  <% if @keypair.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keypair.errors.count, "error") %> prohibited this keypair from being saved:</h2>

      <ul>
      <% @keypair.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="control-group">
      <%= f.label t(:zone), :class => 'control-label' %>
      <div class="controls">
        <% if controller.action_name == "new" %>
          <%= select("keypair", "zone_id", @oeaccount.zones.collect {|p| [p.name, p.id] }, { :prompt => t(:select) }) %>
        <% else %>
          <%= select("keypair", "zone_id", @oeaccount.zones.collect {|p| [p.name, p.id] }, { }, { :disabled => true }) %>
        <% end %>
      </div>
    </div>

   <div class="control-group">
    <div id="zone_select">
    </div>
   </div>

  <div class="field">
    <%= f.label t(:name) %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label t(:public) %><br />
    <%= f.text_field :public %>
  </div>
  <div class="field">
    <%= f.label t(:private) %><br />
    <%= f.text_field :private %>
  </div>
  <div class="field">
    <%= f.label t(:fingerprint) %><br />
    <%= f.text_field :fingerprint %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
