  <% if azonebyzone.nil? %>
  <div class = "field" >
  </div>
  <% else %>
    <div class="control-group">
      <%= label_tag :azone_id, t(:subzone), :class => 'control-label' %>
      <div class="controls">
        <%= select("firewall", "azone_id", azonebyzone.collect{ |p| [p.name, p.id]}, {:prompt => t(:select) }) %>
      </div>
    </div>
  <% end %>

