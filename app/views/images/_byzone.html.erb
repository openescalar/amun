  <% if azonebyzone.nil? %>
  <div class = "control-group" >
  </div>
  <% else %>
    <div class="control-group">
      <%= label :azone_id, "Subzone":class => 'control-label' %>
      <div class="controls">
        <%= select("image", "azone_id", azonebyzone.collect{ |p| [p.name, p.id]}, {:prompt => 'Select Subzone' }) %>
      </div>
    </div>
  <% end %>

