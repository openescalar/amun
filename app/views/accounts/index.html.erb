<h1>Accounts</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Created at</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @accounts[@start..@stop].each do |account| %>
      <tr>
        <td><%= link_to account.name, account_path(account) %></td>
        <td><%= account.created_at %></td>
        <td>
          <%= link_to 'Edit', edit_account_path(account), :class => 'btn btn-mini' %>
          <%= link_to 'Destroy', account_path(account), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New', new_account_path, :class => 'btn btn-primary' %>
<%= render :partial => 'paginate', :locals => {:total => @accounts.size, :page => @page, :objres => "accounts" } %>

